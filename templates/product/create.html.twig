{% extends 'base-front.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
<h1 class="text-center">Création de produit</h1>
    {{ form_start(form)}}
        {{ form_row(form.name)}}
        {{ form_row(form.imagePath) }}
        <button type="submit" class="btn btn-primary d-flex justify-content-center">Valider</button>
    {{form_end(form)}}
    <div class="d-flex justify-content-center"><a href="{{path('app_user_index')}}" class="btn btn-primary mt-5">Votre compte</a></div>
{% endblock %}