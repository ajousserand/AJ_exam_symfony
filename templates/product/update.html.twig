{% extends 'base-front.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
<h1 class="text-center">Mise à jour de produit</h1>
    {{ form_start(form)}}
        {{ form_row(form.name)}}
        {{ form_row(form.imagePath) }}
        <button type="submit" class="btn btn-primary">Valider</button>
    {{form_end(form)}}
{% endblock %}